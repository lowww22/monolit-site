# Как залить / починить деплой на Timeweb

## Важно в панели приложения

1. **Путь проверки состояния:** `/api/health`  
   (не `/` и не пусто — главная тяжёлая, healthcheck не успевает)
2. **Путь до директории проекта:** пусто
3. **Окружение:** Dockerfile
4. **Репозиторий:** `lowww22/monolit-site`, ветка `main`
5. Сертификаты Минцифры: **выкл**

После пуша в `main` дождитесь Redeploy (2–5 мин).

Проверка: `https://ВАШ-ДОМЕН/api/health` → ответ `ok`
